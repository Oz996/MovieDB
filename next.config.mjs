/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "image.tmdb.org",
      "upload.wikimedia.org",
      "media.themoviedb.org",
      "img.youtube.com",
    ],
  },
};

export default nextConfig;
